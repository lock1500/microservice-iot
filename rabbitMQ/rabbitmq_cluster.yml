apiVersion: rabbitmq.com/v1beta1
kind: RabbitmqCluster
metadata:
  name: rabbitmq
spec:
  replicas: 1
  rabbitmq:
    additionalConfig: |
      default_user=guest
      default_pass=guest
      log.console.level = info
    # log.console.level = debug
      
   # additionalPlugins:
    #  - rabbitmq_mqtt